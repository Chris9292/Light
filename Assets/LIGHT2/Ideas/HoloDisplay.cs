using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HoloDisplay : MonoBehaviour
{
   /*[SerializeField] private GameObject[] myPrefabs;
   [SerializeField] private GameObject thePrefab;
   [SerializeField] private Transform ParentTransform;
   [SerializeField] private Vector3 spawnPos;
    private int CurrentPrefab = 0;



public void SpawnDisplay(){

    if(thePrefab != null) {
        Destroy(thePrefab);
    }

    thePrefab = Instantiate(myPrefabs[CurrentPrefab]);
    //thePrefab.transform.parent = ParentTransform;
    thePrefab.transform.SetParent(ParentTransform,false);
    thePrefab.transform.localScale *= 0.1f;
    thePrefab.transform.position += spawnPos; 

        CurrentPrefab += 1;
    
    if (CurrentPrefab >= myPrefabs.Length)
    {
        CurrentPrefab = 0;
        }
    if (CurrentPrefab < 0)
    {
        CurrentPrefab = myPrefabs.Length - 1;
        }
}

*/

}